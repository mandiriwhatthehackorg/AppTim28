import * as React from 'react';
import { ActivityIndicator } from 'react-native-paper';

const Loading = () => <ActivityIndicator animating={true} size={'large'} color="#003D79" />;

export default Loading;
